{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The installer's frontend only talks to its own backend; every privileged action goes through the pkexec helper.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
